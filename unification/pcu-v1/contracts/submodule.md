# PCU-SUBMODULE/1

Project Control is completed, validated, and committed in its standalone repository before adoption. Skills then adds `project-control` as a genuine Git submodule using the existing legitimate remote and pins the exact validated release commit. The parent records only `.gitmodules`, the gitlink, and a release lock. It does not copy Project Control files or history, use a local-only final URL, or delete the standalone checkout.
