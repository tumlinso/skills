# PCU-V1 harness fixtures

These assets are used only inside temporary repositories created by
`tests/pcu_v1`. They contain no live repository path, registration, service
state, environment dump, capability, or credential. The fixture migrator
models only the two migration-owned fields: the marked guidance block and
`configuration.workflow_front_door`.
