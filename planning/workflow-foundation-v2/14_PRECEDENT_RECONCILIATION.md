# 14. What is retained from earlier packages—and what is corrected

The inspected Cellerator JBC, compiler Part One, Semantic Spine, Relation Update Spine, and paired GlassHelix/Cellerator NF1 packages establish the two-layer pattern: one richer authored catalog and one native apply plan, plus human sheets, lane queues, ownership, source evidence and a nonmutating validation path [P01-P04]. Their timestamps and command strings are historical evidence, not current authority.

Retain native schema 3, separate project authorities, exact apply-file naming, deterministic projections, manifests, offline/installed-test distinction, explicit later controller launch, peer import receipts, qualified source identity and preservation of unknown work. Do not use the generic schema-2 compiler on a plan with native lanes.

Carry forward the NF1 coordinator correction: a root epic alone is unclaimable while children remain, so an ordinary coordinator task precedes it. It must not have a prerequisite final milestone, although its completion gate must check that milestone. Carry forward the exclusive integration destination and early test registration corrections [P04].

A new live probe during WF2 construction rejected an epic depending on its own children. Direct graph-source inspection showed that v3 checks parent edges and task prerequisites together [S05]. WF2 has no such epic dependency. A corrected representative PC probe passed, and a minimal Skills schema-3 probe passed, with mutation guards unchanged. Final full native validation remains a separate required on-host step.

Do not duplicate predecessor inefficiencies blindly: workers need current-task sheets, not the whole plan; a source retrieval need not return all worktrees; a convenience provenance note must not masquerade as persisted task semantics. Keep fresh checks, but design future scoped references instead of making the model re-read irrelevant identities repeatedly.

The Codex façade is now `/home/tumlinson/codex-workspace`, with live symlinks into `~/.codex`. Preserve this separation and actual target bytes. The inspected current root model is Sol/medium. Earlier conversational model examples were policy suggestions, not evidence of the installed runtime. Verify actual child model, MCP and permissions before relying on routing.
