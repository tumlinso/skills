interface:
  display_name: "CUDA"
  short_description: "CUDA correctness, profiling, and GPU optimization"
  default_prompt: "Use $cuda to route CUDA correctness, build, profiling, debugging, and architecture-specific optimization work with host-global GPU interlocks."
policy:
  allow_implicit_invocation: true
