interface:
  display_name: "C++ Context Compiler"
  short_description: "Budgeted semantic C++ context slices"
  default_prompt: "Use $cpp-context-compiler to initialize the repository if needed, resolve a C++ target, and generate the smallest sufficient source-mapped context bundle."
