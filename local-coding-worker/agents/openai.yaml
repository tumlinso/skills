interface:
  display_name: "Local Coding Worker"
  short_description: "Bounded todo-authorized local coding delegation"
  default_prompt: "Use $local-coding-worker for one bounded child execution authorized by the active todo parent claim, then return compact evidence."
policy:
  allow_implicit_invocation: true
