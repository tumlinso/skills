version: 1
display_name: CUDA V100
short_description: V100 CUDA entry point with routing for crash triage, debugger helpers, CPU-porting, Tensor Cores, request-only PTX hot-path inspection, large-data benchmarks, and profiler decisions
default_prompt: Assist with CUDA build, crash triage, debugger helper workflows, profiling, CPU-to-CUDA porting, Tensor Core routing, request-only PTX guidance, kernel fusion, divergence and launch-overhead tradeoffs, memory-tier choice, fit, topology, pipeline, benchmark standardization, large-data benchmark saturation, Torch extension, and performance work for code intended to run on NVIDIA Tesla V100 GPUs. Choose the dominant bottleneck first, load only the matching addendum or benchmark references, prefer compact crash, benchmark, profiler, or PTX-dump summaries over raw artifacts when possible, isolate hot CUDA paths before deep PTX or SASS inspection, and use the skill scripts to keep output low-context.
