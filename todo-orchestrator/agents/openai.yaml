interface:
  display_name: "Todo Orchestrator"
  short_description: "Transactional parallel task pickup, interlocks, resources, and evidence"
  default_prompt: "Use $todo-orchestrator and continue. Bootstrap the project, atomically claim safe ready work, follow the compact task capsule, and finish through the structured CLI."
policy:
  allow_implicit_invocation: true
